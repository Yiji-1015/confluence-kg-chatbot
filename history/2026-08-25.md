# 2026-08-25 작업일지 — 포트폴리오 최적화: 계층형 모듈러 Docker Compose 아키텍처 및 문서화

## 기록 원칙
- `history/`: 날짜별 진행 과정, 주요 설계 질문/답변, 결정 사항, 다음 작업
- 파일명: `YYYY-MM-DD.md`

---

## 1. 오늘 진행한 핵심 작업

### 1-1. 포트폴리오 관점 아키텍처 재정립
- **목표**: 단순히 "다양한 도구를 써봤다"는 나열형 구조에서 탈피하여, **제한된 자원(PoC/개인 개발 환경)에서 실무적으로 필요한 컴포넌트를 선별하고 단계적으로 확장 가능한 계층형 아키텍처(Modular Layered Architecture)**를 설계하고 문서화.
- **핵심 메시지**:
  - 도구의 존재 이유와 트레이드오프 정의 (왜 필요한가? 언제 배제 가능한가?)
  - 상시 필수 구동(Core/App)과 선택적 구동(Search/KG/Obs) 분리
  - Polyglot Persistence, Dynamic Routing & Fallback, OTel 관측성 등의 실무형 운영 설계 부각

### 1-2. 계층별 Docker Compose 파일 분리
기존의 무거운 단일 `docker-compose.yml`을 5개의 목적별 파일로 분리하고 공통 네트워크(`confluence-net`)로 연결:

1. `docker-compose.yml`: **[Core Layer]** PostgreSQL 16 (영구 저장) + Redis 7 (세션 캐시) 및 공통 브리지 네트워크 선언 (~1GB RAM)
2. `docker-compose.app.yml`: **[App Layer]** Spring Boot 백엔드 + FastAPI AI 엔진 컨테이너화 (~1.5GB RAM)
3. `docker-compose.search.yml`: **[Search Layer]** Elasticsearch 8.15(Nori 형태소 분석기 + Dense Vector) + Kibana UI + Setup (~3GB RAM)
4. `docker-compose.kg.yml`: **[KG Layer]** Neo4j 5 Community 지식 그래프 DB (~2GB RAM)
5. `docker-compose.obs.yml`: **[Observability/Gateway Layer]** LiteLLM 라우팅 게이트웨이 (~0.8GB RAM)

### 1-3. FastAPI AI 엔진 컨테이너화 지원
- `ai-server/Dockerfile`: Python 3.12-slim 기반 경량 컨테이너 이미지 빌드 및 `/internal/health` 헬스체크 설정
- `ai-server/.dockerignore`: 캐시, 가상환경, 테스트 데이터 제외

### 1-4. 포트폴리오 문서 및 가이드 고도화
- `README.md`:
  - 포트폴리오 핵심 설계 철학 전면 배치
  - 계층별 리소스 매트릭스(메모리 풋프린트 & 트레이드오프) 정리
  - 단계별 실행 가이드(Tier 1 Core 개발 ~ Tier 4 Full Stack 통합) 작성
- `docs/ARCHITECTURE.md`: 기술 면접 대비용 Architecture Decision Records(ADR-001 ~ ADR-005) 상세 작성
- `docs/COMMANDS.md`: 일상 개발, Docker 조작, DB 직접 접속(psql/redis-cli), 모니터링(`docker stats`), 평가 스크립트 실행 등을 총망라한 통합 치트시트 작성

### 1-5. Docker Desktop 실기동 및 엔드투엔드 파이프라인 검증
- **전체 컨테이너 동시 정상 기동 확인**:
  - `kg-postgres` (16-alpine): 포트 5432 (Healthy)
  - `kg-redis` (7-alpine): 포트 6379 (Healthy)
  - `kg-elasticsearch` (8.15.0 + Nori): 포트 9200 (Green, Healthy)
  - `kg-kibana` (8.15.0): 포트 5601 (HTTP 200)
  - `kg-neo4j` (5-community): 포트 7474 / 7687 (Running)
  - `kg-litellm` (main-latest): 포트 4000 (Liveliness "I'm alive!")
  - `kg-backend` (Spring Boot 3 / JDK 21 JRE): 포트 8080 (HTTP 200 Web UI)
  - `kg-ai-server` (FastAPI / Python 3.12): 포트 8000 (Healthy)
- **엔드투엔드 채팅 API 검증**:
  - `POST /api/chat` ("안녕하세요") 요청 성공 및 세션 자동 발급
  - Spring Boot 세션 오케스트레이션 ↔ Redis 세션 캐시 ↔ PostgreSQL 영구 이력 ↔ FastAPI AI 서버 비동기 응답 정상 동작 확인.

---

## 2. 주요 Q&A 및 설계 결정

### Q1. "Compose 파일을 분리하면 컨테이너 간 통신은 어떻게 되는가?"
- 모든 Compose 파일에 `networks: default: name: confluence-net`을 정의하여, 독립 실행하든 `-f` 옵션으로 조합 실행하든 동일한 `confluence-net` 브리지 네트워크에 조인됨.
- 따라서 `postgres`, `redis`, `elasticsearch`, `litellm`, `neo4j` 호스트명으로 컨테이너 간 완벽한 통신이 보장됨.

### Q2. "로컬 개발과 Docker 배포 시 포트 충돌은 없는가?"
- 호스트 포트 포워딩(`5432`, `6379`, `8000`, `8080`, `9200` 등)을 유지하여, Docker로 앱을 띄우지 않고 로컬 호스트에서 `./gradlew bootRun` 및 `uvicorn`을 실행할 때도 설정 변경 없이 즉시 연동 가능.

---

## 3. 다음 예정 작업
1. Knowledge Graph (Phase 5) — Neo4j 엔티티/관계 모델링 및 Hybrid Retrieval과 Graph Context 결합
2. 프론트엔드 고도화 및 배포 (Phase 7 / Vercel 배포 연동)
