# 평가 전용 의존성. 서빙에는 필요 없어서 requirements.txt와 분리했다.
# ragas는 langchain / langgraph / datasets 등 50개+ 패키지를 끌고 오므로
# 이걸 서빙 이미지에 넣으면 런타임이 불필요하게 무거워진다.
#
# 설치 (PowerShell / bash 공통):
#   docker exec rag-ai-server pip install -r /app/requirements-eval.txt
# 컨테이너 재생성 시 사라지므로 평가 전에 매번 실행한다.
#
# 평가 점수는 전부 이 패키지가 계산한다 (run_qa.py에는 채점 로직이 없다).
# 따라서 버전을 올리면 지표 구현이 바뀌어 이전 실행과 비교할 수 없게 된다.
# 올릴 때는 EVALUATION.md 5장의 기준선을 같이 다시 잰다.
# openai / langchain-core 등은 ragas가 의존성으로 끌고 오므로 따로 적지 않는다.

ragas==0.4.3

# ragas 0.4.3이 langchain-community를 버전 제한 없이 선언하는데,
# 0.4.x에서 제거된 langchain_community.chat_models.vertexai를 참조해 import가 깨진다.
# 0.3.x로 고정해야 동작한다 (langchain-core는 건드리지 않음).
langchain-community<0.4
